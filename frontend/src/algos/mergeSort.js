import mergeImage from '../images/merge_img.svg'

const algo = {}

algo.name = 'Merge Sort'
algo.routeName = 'mergeSort'

algo.replitCode = "https://replit.com/@EmpatheticLions/Merge-Sort?embed=true";

algo.description = 'Merge sort is one of the most efficient sorting algorithms. It works on the principle of Divide and Conquer. Merge sort repeatedly breaks down a list into several sublists until each sublist consists of a single element and merging those sublists in a manner that results into a sorted list.'

algo.image = mergeImage

algo.function = function merge(data) {
    
}


export default algo